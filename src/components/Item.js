import React from "react";
import "./Item.css";

function Item(props) {
	return (
		<div>
			{/* <img src={props.data.img} />
		<h3>{props.data.title}</h3>
		<p>{props.data.text}</p>
		<button>BUTTON!</button> */}
		</div>
	);
}

export default Item;
